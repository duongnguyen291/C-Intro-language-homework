#include <stdio.h>
int convert(int *n);
int main(){
unsigned int n;
printf("\nEnter the number n: ");
scanf("%d",&n);
printf("\nConvert number is: %d",convert(&n));
return  0;    
}
int convert(int *n){
    int u;
    int unit=0;
    while(n>0){

    }
    return u;
}